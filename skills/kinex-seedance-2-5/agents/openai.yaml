interface:
  display_name: "Direct Seedance"
  short_description: "Default to Seedance 2.0 and direct references deliberately"
  default_prompt: "Use $kinex-seedance-2-5 to direct these approved references through Kinex, defaulting to full-quality Seedance 2.0 unless I explicitly request another Seedance SKU."
dependencies:
  tools:
    - type: "mcp"
      value: "kinex"
      description: "Kinex creative production MCP server"
      transport: "streamable_http"
      url: "https://api.kinex.studio/mcp"
