interface:
  display_name: 'Direct Agent Workspace'
  short_description: 'Source-grounded, variant-aware production direction'
  default_prompt: 'Use $kinex-agent-workspace to reconcile the current plan, preserve explicit hero and anchor locks, and continue approved production through verified media.'
dependencies:
  tools:
    - type: 'mcp'
      value: 'kinex'
      description: 'Kinex creative production MCP server'
      transport: 'streamable_http'
      url: 'https://api.kinex.studio/mcp'
