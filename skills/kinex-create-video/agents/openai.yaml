interface:
  display_name: 'Create a Kinex Video'
  short_description: 'Take a brief to a verified final cut'
  default_prompt: 'Use $kinex-create-video to turn this brief into a finished Kinex video.'
dependencies:
  tools:
    - type: 'mcp'
      value: 'kinex'
      description: 'Kinex creative production MCP server'
      transport: 'streamable_http'
      url: 'https://api.kinex.studio/mcp'
